# Handoff — стабильность claude-туннеля

Создан 2026-05-19, обновлён 2026-05-19 ~14:10 после первого разбора логов.

## TL;DR что было 2026-05-18

- Два провала: **10:33–13:00** (пропал интерфейс `wg-tunnel`, ~2ч27м) и **15:47–16:14** (~27 мин, «тихий» отказ, причина не установлена).
- Найдено и устранено: панель `x-ui` крутилась в crash-loop (конфликт портов с `xray.service`) — остановлена и `disabled`.
- Поставлен серверный read-only логгер стабильности.
- Полный разбор: `docs/superpowers/diagnostics/2026-05-18-tunnel-outage-report.md` (раздел 11 — errata с подтверждёнными фактами).

## TL;DR что разобрано 2026-05-19

- Логи проверены за сутки работы логгера. Полный отчёт: **`docs/superpowers/diagnostics/2026-05-19-tunnel-observations.md`**.
- Сервер: **uptime 99.23%** (10 FAIL / 1307 проб). Деградаций уровня 2026-05-18 нет; `iface=DOWN` не было ни разу; ничего на ru-vds не падало.
- Сбои делятся на два класса. **Класс A** (ru-vds→Hetzner) — все 10 серверных FAIL, короткие (1–4 мин), самовосстанавливаются; корень за пределами ru-vds. **Класс B** (macOS→ru-vds) — отвал SSH-master, локальная сторона.
- Инцидент, замеченный пользователем ~13:09 — класс A: блип WG + onward-отказ Hetzner-ноды.

## ПЕРВЫМ ДЕЛОМ в новой сессии — Hetzner-сервер

Корень класса A — на Hetzner-ноде (`95.217.128.117`), доступа к ней у прошлых сессий не было. Пользователь планировал зайти на неё из дома. Смотреть: `wg show` (хендшейки/потери на peer-стороне), onward-маршрут и NAT в интернет, потери на uplink, `journalctl`/`dmesg` за окна серверных FAIL (13:09–13:12, 01:22, 02:53, 04:05, 09:34 MSK 2026-05-19).

### Команды для повторной сверки логов

Доступ к ru-vds (read-only, разрешён пользователем): `ssh root@claude-proxy`.

Серверный логгер (копится раз в минуту):
```
ssh root@claude-proxy 'awk "{n++}/FAIL/{f++}END{printf \"uptime: %.2f%% (%d FAIL / %d проб)\n\",(n-f)/n*100,f+0,n}" /var/log/wg-tunnel-stat.log; echo "--- последние FAIL ---"; grep FAIL /var/log/wg-tunnel-stat.log | tail -30'
```

Клиентский лог (переходы статуса):
```
grep -E "^2026-05-(18|19)T" ~/.claude-wrapper/tunnel.log | awk '{c=($0~/FAIL/)?"FAIL":"OK"; if(c!=p){print;p=c}}'
```

Сопоставлять по таймстампам: FAIL в обоих логах = звено `ru-vds→Hetzner` (класс A); FAIL только на клиенте = `macOS→ru-vds` (класс B).

Как читать поля серверного лога: `iface=DOWN` → пропал интерфейс; `ping=FAIL` при `iface=up` → WG-сессия мертва; `e2e=000` при `ping=ok` → проблема в Xray/upstream; `hs_age` растёт за ~180с → деградация WG.

## Текущее состояние сервера (на 2026-05-18 ~17:00)

- `xray.service` — обслуживает VPN семьи (`:443`) и claude (`:18080`), конфиг `/etc/xray-config.json`. Здоров.
- `x-ui.service` — остановлен + `disabled`. Наобум не реанимировать (его база — 1 клиент из 7).
- Логгер: `/usr/local/bin/wg-tunnel-stat.sh`, cron `* * * * *`, лог `/var/log/wg-tunnel-stat.log`. Исходник в репо: `extras/server/wg-tunnel-stat.sh`.
- Туннель к Hetzner = интерфейс **`wg-tunnel`** (`10.88.88.2`), НЕ `wg0`.
- Команда восстановления туннеля: `systemctl restart wg-quick@wg-tunnel`.
- GOTCHA: `xray.service` запускает бинарь `/usr/local/x-ui/bin/xray-linux-amd64` — каталог `/usr/local/x-ui` НЕ удалять.

## Открытые вопросы / решения в ожидании

1. **Авто-восстановление `wg-quick@wg-tunnel`** — по данным за сутки НЕ внедрять: `iface=DOWN` не повторялся, юнит не падал, healthcheck по `ip link` этот класс сбоев не лечит. Подробнее — раздел «Вывод» в отчёте 2026-05-19. Пересмотреть, только если `iface=DOWN` повторится.
2. **Провал 15:47–16:14 (2026-05-18)** — за сутки логгер повтора такой длительности НЕ поймал. Все сбои 2026-05-19 — короткие (1–4 мин). Причина исходного провала так и не установлена.
3. **Корень класса A — Hetzner-нода.** Диагностику делает пользователь из дома (см. «ПЕРВЫМ ДЕЛОМ» выше).
4. Незакоммиченные изменения в рабочем дереве (пользователь просил НЕ коммитить): `extras/server/wg-tunnel-stat.sh`, errata в отчёте 2026-05-18, отчёт 2026-05-19.

## Ограничения

- Любое изменение на сервере — только с явного согласия пользователя на каждый шаг.
- Ничего не коммитить без отдельной команды. `git push` — никогда без явного указания.
